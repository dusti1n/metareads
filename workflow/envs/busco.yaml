# Conda_ENV; busco; for all data types
name: busco
channels:
  - bioconda
  - defaults
  - conda-forge
dependencies:
# busco_dependent_packages
  - busco=6.0.0
# buscoplot.R_depent_packages
  - r-base=4.1.3
  - r-reshape2=1.4.4
  - r-ggplot2=3.4.2
  - r-tidyverse=1.3.2
  - r-conflicted=1.2.0
# buscosum.R_depent_packages
  - r-gridextra=2.3